<p style="color: green"><%= notice %></p>

<div class="mx-5 mt-2">
  <p>Code de réservation : <%= @reservation.code %></p>
  <p>Classe du/des siège(s) : <%= @reservation.seat_class_choice.name %></p>
  <p>Nombre de(s) siège(s) : <%= @reservation.seat_number %></p>
  <p>Vol : <%= @reservation.flight.arrival_airport %> - <%= @reservation.flight.departure_airport %> (<%=  l @reservation.flight.departure_date, format: :default_without_second %> - <%= l @reservation.flight.departure_date + @reservation.flight.duration.minutes, format: :default_without_second %>)</p>

  <div>
    <%= link_to "Retour à la liste des réservations.", reservations_path %>
  </div>

  <div class="mt-4 text-end">
    <%= button_to "Supprimer la réservation", @reservation, method: :delete, class: "btn btn-danger" %>
  </div>

</div>
