<%= form_with(model: seat_class_choice) do |form| %>
  <% if seat_class_choice.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(seat_class_choice.errors.count, "error") %> prohibited this seat_class_choice from being saved:</h2>

      <ul>
        <% seat_class_choice.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
